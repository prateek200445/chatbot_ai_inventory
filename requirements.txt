requests>=2.31.0,<3.0.0
tabulate>=0.9.0,<1.0.0
# No additional requirements for GUI as tkinter is included with Python

# Requirements for web deployment
streamlit>=1.31.0,<2.0.0

# Remove PyInstaller for web deployment - it's only needed for desktop applications
# and it's causing compatibility issues on Render